apiVersion: v1
kind: Service
metadata:
  name: {{ include "haanflix.fullname" . }}
  labels:
    {{- include "haanflix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "haanflix.selectorLabels" . | nindent 4 }}
